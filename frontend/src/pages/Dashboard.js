import React from 'react';

export default function Dashboard(){
  return (
    <div>
      <h2>Hospital Management Dashboard</h2>
      <p>This is a minimal scaffold. Use the API endpoints in <code>/api</code> to connect features.</p>
    </div>
  );
}
